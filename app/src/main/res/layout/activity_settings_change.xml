<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.nepal.naxa.smartnaari.setingschange.SettingsChangeActivity">

    <include layout="@layout/toolbar_layout"
        android:id="@+id/toolbarLayoutHeader"/>

    <ScrollView
        android:layout_below="@id/toolbarLayoutHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="@dimen/activity_horizontal_margin">

    <Button
        android:id="@+id/settings_enable_disable_mycircle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/button_get_contact_my_circle"
        android:text="Enable/Disable MyCircle"
        android:textColor="@color/colorWhite"
        android:padding="@dimen/default_padding"/>

    </LinearLayout>
    </ScrollView>

</RelativeLayout>
